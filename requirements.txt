# Core dependencies
openai>=1.0.0
python-dotenv>=1.0.0
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0

# For async operations
asyncio-throttle>=1.0.0

# For HuggingFace models
transformers>=4.30.0
torch>=2.0.0
accelerate>=0.26.0
datasets>=2.0.0

# vLLM for efficient model serving
vllm>=0.6.0
requests>=2.31.0  # For vLLM health checks

# Optional but recommended
ipython>=8.0.0
jupyter>=1.0.0
